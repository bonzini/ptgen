// Remember that the lighten operator works separately on each component,
// so we keep the 1 red and green components of SRC2: the result is SRC2
// (yellow -> white)

let SRC1 := create image from f(X,Y)=X;
let SRC2 := create image from f(X,Y)=1-X;

apply SRC2 to SRC1 operation lighten
